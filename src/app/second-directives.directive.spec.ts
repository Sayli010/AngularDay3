import { SecondDirectivesDirective } from './second-directives.directive';

describe('SecondDirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new SecondDirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
